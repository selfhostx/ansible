settings {
  accept_logs {
    id = "accept_logs";
    priority = "high";
    from = "root@{{ rspamd_domain }}";
    rcpt = "root@{{ rspamd_domain }}";
    apply {
      groups_disabled = ["surbl"];
      symbols_disabled = ["R_SUSPICIOUS_URL"];
    }
  }
}
